{
  "rules": {
    "drafts": {
      ".read": true,
      "$draft": {
        "order": {
          ".write": "root.child('admins').child(auth.uid).exists()"
        },
        "picks": {
          ".write": "auth != null"
        }
      }
    },
    "users": {
      ".read": true,
      "$uid": {
        ".write": "$uid === auth.uid"
      }
    },
    "watchlists": {
      "$draft": {
        "$uid": {
          ".read": "$uid === auth.uid",
          ".write": "$uid === auth.uid"
        }
      }
    },
    "trades": {
      "$draft": {
        "$trade": {
          ".write": "root.child('drafts').child($draft).child('teams').child(auth.uid).exists()",
          ".read": "data.child('receivingTeam').val() === auth.uid || data.child('givingTeam').val() === auth.uid",
        }
      }
    },
    "tradesUsersPivot": {
      "$draft": {
        "$uid": {
          ".read": "$uid === auth.uid",
          ".write": "!data.exists() && $uid === auth.uid",
          "$trade": {
            ".write": "root.child('trades').child($draft).child($trade).child('receivingTeam').val() === auth.uid ||
            root.child('trades').child($draft).child($trade).child('givingTeam').val() === auth.uid"
          }
        }
      }
    },
    "tradesAccepted": {
      ".read": true,
      "$draft": {
        "$trade": {
          ".write": "root.child('drafts').child($draft).child('teams').child(auth.uid).exists() && !data.exists()",
        }
      }
    }
  }
}
